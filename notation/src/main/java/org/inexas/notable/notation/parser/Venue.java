package org.inexas.notable.notation.parser;

import org.inexas.notable.notation.model.*;

/**
 * A place where Events are held (see what I did there)
 */
public interface Venue {
	void add(Event event);
}
